"""LinkedIn engagement automation package.

Finds relevant feed posts, drafts authentic first-person comments, posts them
with human-like browser behavior, sends connection requests, and publishes
thought-leadership posts — all driven by a per-profile lifecycle store and an
optional randomized scheduler. The Flask dashboard (``dashboard`` module) is the
entry point; ``python -m linkedin_automation.dashboard`` launches it on :6500.
"""
